version: "3.9"

services:

  ai-recruitment-platform:

    build: .

    container_name: ai-recruitment-platform

    ports:
      - "8501:8501"

    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}

    volumes:
      - .:/app

    restart: unless-stopped